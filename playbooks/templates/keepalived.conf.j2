vrrp_instance VI_1 {
    state {{ 'MASTER' if inventory_hostname == 'Pihole01' else 'BACKUP' }}
    interface eth0
    virtual_router_id 51
    priority {{ 100 if inventory_hostname == 'Pihole01' else 90 }}
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass your_secure_password
    }
    virtual_ipaddress {
        192.168.2.2
    }
}
